const TIMEOUT = 800;
const LIVES_COUNT = 3;

const AppRoute = {
    Game: 'game',
    Results: 'results',
}

window.TIMEOUT = TIMEOUT;
window.LIVES_COUNT = LIVES_COUNT;
window.AppRoute = AppRoute;